{
  "name": "2022-patchwork-automata",
  "version": "1.0.0",
  "description": "The main Patchwork Automata website.",
  "scripts": {
    "preprocess": "rimraf content-merged && cpy . '../content-merged/' --cwd=content-public && cpy . '../content-merged/' --cwd=content-private",
    "build": "npm run preprocess && npx @11ty/eleventy",
    "start": "npm run preprocess && npx @11ty/eleventy --serve",
    "dev": "netlify dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": {
    "name": "Steven Lehrburger",
    "email": "lehrburger@gmail.com"
  },
  "devDependencies": {
    "netlify-cli": "^6.15.0"
  },
  "dependencies": {
    "@11ty/eleventy": "^1.0.1",
    "cookie": "^0.4.1",
    "cpy-cli": "^4.1.0",
    "dotenv": "^10.0.0",
    "node-fetch": "^2.3.0",
    "rimraf": "^3.0.2",
    "simple-oauth2": "^4.2.0"
  }
}
